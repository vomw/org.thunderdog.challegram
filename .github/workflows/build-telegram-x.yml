name: Build & Sign Telegram X APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    name: Build Signed APK
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      # ─── 1. Checkout repository ──────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: TGX-Android/Telegram-X
          fetch-depth: 0
          submodules: false
          lfs: true

      # ─── 2. Java 21 ────────────────────────────────────────────────────────
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      # ─── 3. Android SDK + NDK + CMake ──────────────────────────────────────
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK and Build Tools
        run: |
          chmod +x scripts/*.sh || true
          
          # Print version.properties for diagnostics
          cat version.properties || echo "version.properties NOT FOUND"
          
          # Dynamically determine required NDK from version.properties
          P_NDK=""
          L_NDK=""
          if [ -f scripts/read-property.sh ] && [ -f version.properties ]; then
            P_NDK=$(./scripts/read-property.sh version.properties version.ndk_primary || true)
            L_NDK=$(./scripts/read-property.sh version.properties version.ndk_legacy || true)
          fi
          
          PRIMARY_NDK=${P_NDK:-"23.2.8568313"}
          LEGACY_NDK=${L_NDK:-"23.2.8568313"}
          
          echo "Installing NDK versions: $PRIMARY_NDK, $LEGACY_NDK"
          yes | sdkmanager --install \
            "ndk;$PRIMARY_NDK" \
            "ndk;$LEGACY_NDK" \
            "cmake;3.22.1" \
            "build-tools;36.0.0" \
            "platforms;android-36" \
            "platforms;android-35" \
            "platforms;android-34"

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y yasm wget unzip git python3 pkg-config libtool gettext ninja-build meson gperf bison flex
          # Git Optimizations
          git config --global http.postBuffer 1048576000 # 1GB
          git config --global core.compression 0
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999

      # ─── 4. Cache native build outputs ─────────────────────────────────────
      - name: Cache native libraries
        id: native-cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            app/.cxx
            app/jni/build
            app/src/main/libs
            app/jni/third_party/ffmpeg/build
            app/jni/third_party/libvpx/build
          key: native-v10-${{ runner.os }}-${{ hashFiles('version.properties', 'scripts/private/*.sh') }}
          restore-keys: |
            native-v10-${{ runner.os }}-

      # ─── 5. Robust Dependency Setup ────────────────────────────────────────
      - name: Setup Dependencies
        run: |
          set -e
          set -x
          
          git config --global url."https://github.com/".insteadOf "git@github.com:" || true
          
          # 5.1 Make all scripts executable
          chmod +x scripts/*.sh scripts/private/*.sh || true
          
          # 5.2 Patch scripts for ARM64 Latest build
          python3 -c '
          import os, re
          
          def patch_file_regex(path, pattern, replace):
              if not os.path.exists(path): return
              with open(path, "r") as f: content = f.read()
              new_content = re.sub(pattern, replace, content)
              if new_content != content:
                  with open(path, "w") as f: f.write(new_content)
                  print(f"Patched {path}")
              else:
                  print(f"PATTERN NOT MATCHED in {path}")

          # Fix set-env.sh typo
          patch_file_regex("scripts/set-env.sh", r"ANDROID_SDK_ROOT=$DEFAULT_ANDROID_SDK\b", "ANDROID_SDK_ROOT=$DEFAULT_ANDROID_SDK_ROOT")
          
          # Force ARM64 Latest build for speed
          patch_file_regex("scripts/private/build-vpx-impl.sh", r"for ABI in .+ ; do", "for ABI in arm64-v8a ; do")
          patch_file_regex("scripts/private/build-vpx-impl.sh", r"for FLAVOR in .+ ; do", "for FLAVOR in latest ; do")
          
          ffmpeg_script = "scripts/private/build-ffmpeg-impl.sh"
          if os.path.exists(ffmpeg_script):
              with open(ffmpeg_script, "r") as f: lines = f.readlines()
              with open(ffmpeg_script, "w") as f:
                  done = False
                  for line in lines:
                      f.write(line)
                      if "build_one" in line and not done and "function" not in line:
                          f.write("exit 0\n")
                          done = True
          '
          
          # Disable reset.sh to protect cache
          echo "#!/bin/bash" > scripts/reset.sh
          echo "echo 'Skipping reset...'" >> scripts/reset.sh
          chmod +x scripts/reset.sh

          # 5.3 Surgical Submodule Sync (with deep cleanup)
          if [ -f .gitmodules ]; then
            echo "Starting surgical submodule sync..."
            SUBMODULES=(
              "app/jni/third_party/ffmpeg"
              "app/jni/third_party/libvpx"
              "app/jni/third_party/opus"
              "app/jni/third_party/webp"
              "app/jni/third_party/libyuv"
              "app/jni/third_party/lz4"
              "app/jni/third_party/jni-utils"
              "app/jni/third_party/rlottie"
              "tdlib"
              "vkryl/leveldb"
              "vkryl/core"
              "vkryl/td"
              "vkryl/android"
              "app/jni/tgvoip/third_party/libtgvoip"
              "app/jni/tgvoip/third_party/webrtc"
              "app/jni/tgvoip/third_party/tgcalls"
              "app/jni/tgvoip/third_party/rnnoise"
              "app/jni/tgvoip/third_party/libsrtp"
              "app/jni/tgvoip/third_party/usrsctp"
              "app/jni/tgvoip/third_party/openh264"
              "app/jni/tgvoip/third_party/abseil-cpp"
              "app/jni/tgvoip/third_party/libevent"
              "app/jni/tgvoip/third_party/crc32c"
              "app/jni/tgvoip/third_party/webrtc_deps/base"
              "app/jni/tgvoip/third_party/webrtc_deps/third_party"
              "thirdparty/androidx-media/latest"
              "thirdparty/androidx-media/legacy"
              "thirdparty/androidx-media/lollipop"
            )
            
            git submodule init
            for sm in "${SUBMODULES[@]}"; do
              if git config --file .gitmodules --get-regexp "path" | grep -q "$sm"; then
                echo "Syncing $sm..."
                # Tiered sync: shallow -> full -> fresh clone
                git submodule update --init --recursive --force --depth 1 "$sm" || \
                git submodule update --init --recursive --force "$sm" || {
                  echo "Retry 3 for $sm (Purging directory)..."
                  git submodule deinit -f "$sm" || true
                  rm -rf "$sm"
                  git submodule update --init --recursive --force "$sm"
                }
              fi
            done
          fi

          # 5.4 Environment Setup
          export ANDROID_SDK_ROOT=$ANDROID_HOME
          CPU_COUNT=$(nproc --all)
          
          cat > local.properties <<EOF
          sdk.dir=$ANDROID_HOME
          org.gradle.workers.max=$CPU_COUNT
          telegram.api_id=${{ secrets.TELEGRAM_API_ID || '12345' }}
          telegram.api_hash=${{ secrets.TELEGRAM_API_HASH || 'abcdef0123456789abcdef0123456789' }}
          app.id=org.thunderdog.challegram
          app.name=Telegram X
          app.download_url=https://github.com/${{ github.repository }}
          app.sources_url=https://github.com/${{ github.repository }}
          keystore.file=$(pwd)/keystore.jks
          EOF
          
          # 5.5 Build Native (if not cached)
          if [ "${{ steps.native-cache.outputs.cache-hit }}" != "true" ]; then
            ./scripts/setup.sh --skip-sdk-setup
          else
            echo "Cache hit, skipping setup.sh and running patches manually..."
            export SED="sed"
            export THIRDPARTY_LIBRARIES="$(pwd)/app/jni/third_party"
            ./scripts/private/patch-opus-impl.sh || true
            ./scripts/private/patch-androidx-media-impl.sh || true
          fi
        env:
          ANDROID_NDK_VERSION_PRIMARY: 23.2.8568313
          ANDROID_NDK_VERSION_LEGACY: 23.2.8568313

      # ─── 6. Verify and Final Config ────────────────────────────────────────
      - name: Verify results and Final Config
        run: |
          ls -R app/jni/third_party/ffmpeg/build/latest/arm64-v8a/include || echo "FFmpeg headers NOT FOUND"
          cat > local.properties <<EOF
          sdk.dir=${ANDROID_SDK_ROOT}
          org.gradle.workers.max=$(nproc --all)
          telegram.api_id=${{ secrets.TELEGRAM_API_ID }}
          telegram.api_hash=${{ secrets.TELEGRAM_API_HASH }}
          EOF

      # ─── 7. Keystore ───────────────────────────────────────────────────────
      - name: Decode keystore
        run: |
          if [ -z "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "Generating dummy keystore..."
            keytool -genkey -v -keystore keystore.jks -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          else
            echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > keystore.jks
          fi

      # ─── 8. Build ──────────────────────────────────────────────────────────
      - name: Build APK
        run: |
          chmod +x ./gradlew
          ./gradlew assembleLatestArm64Release --stacktrace
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_SDK_ROOT }}/ndk/23.2.8568313
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}

      # ─── 9. Sign ───────────────────────────────────────────────────────────
      - name: Sign APK
        run: |
          APK_PATH=$(find app/build/outputs/apk -name "*.apk" | grep "release" | head -1)
          if [ -z "$APK_PATH" ]; then exit 1; fi
          
          KS_PASS=${{ secrets.KEYSTORE_PASSWORD || 'android' }}
          KS_ALIAS=${{ secrets.KEY_ALIAS || 'androiddebugkey' }}
          KEY_PASS=${{ secrets.KEY_PASSWORD || 'android' }}

          $ANDROID_SDK_ROOT/build-tools/36.0.0/apksigner sign \
            --ks keystore.jks \
            --ks-pass pass:"$KS_PASS" \
            --ks-key-alias "$KS_ALIAS" \
            --key-pass pass:"$KEY_PASS" \
            --out app-release-signed.apk \
            "$APK_PATH"

      # ─── 10. Upload ────────────────────────────────────────────────────────
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: telegram-x-signed-${{ github.run_number }}
          path: app-release-signed.apk
          retention-days: 30
